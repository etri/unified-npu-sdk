"""
Unified SDK (RBLN-only worktree)
================================

A unified SDK for compiling and running AI models on Rebellions NPUs.

Structure:
 - build:     Model compilation modules
 - runtime:   Runtime creation and inference modules
 - backends:  Backend adapters (RBLN only in this worktree)
 - frontends: Model import and conversion (PyTorch, etc.)
"""

__version__ = "0.1.0"
